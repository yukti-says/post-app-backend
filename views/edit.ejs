<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Post App</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/feather-icons"></script>
  </head>
  <body>
    <div class="main bg-zinc-900 w-full min-h-screen text-white p-10">

      <h4 class="font-bold text-xl mt-3">Edit Your Post</h4>

      <form action="/update/<%= post._id%>" method="post">
        <textarea
          class="w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-500 rounded-md mt-5  "
          
          name="content"
  
         ><%= post.content%>
    </textarea>
        <input
          class="px-3 py-2 bg-yellow-500 hover:bg-yellow-700 block rounded-md mt-2 w-40 text-sm font-bold"
          type="submit"
          value="Update post"
        />
      </form>
    </div>
  </body>
</html>
