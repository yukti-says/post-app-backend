<!-- views/index.ejs
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Register</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white flex flex-col items-center justify-center min-h-screen">
  <div class="bg-zinc-800 p-10 rounded-lg shadow-md w-full max-w-md">
    <h2 class="text-3xl font-bold mb-5">Create Account</h2>
    <form action="/register" method="post" class="space-y-4">
      <input class="input" type="text" name="username" placeholder="Username" required />
      <input class="input" type="text" name="name" placeholder="Name" required />
      <input class="input" type="number" name="age" min="1" max="120" placeholder="Age" required />
      <input class="input" type="email" name="email" placeholder="Email" required />
      <input class="input" type="password" name="password" placeholder="Password" required />
      <button class="btn-primary w-full" type="submit">Register</button>
    </form>
    <p class="text-sm mt-4 text-zinc-400">Already have an account? 
      <a href="/login" class="text-blue-400 underline">Login</a>
    </p>
  </div>

  <style>
    .input {
      width: 100%;
      padding: 0.5rem;
      background: transparent;
      border: 1px solid #444;
      border-radius: 0.375rem;
      outline: none;
    }
    .btn-primary {
      background-color: #3b82f6;
      padding: 0.5rem;
      border-radius: 0.375rem;
      font-weight: bold;
    }
    .btn-primary:hover {
      background-color: #2563eb;
    }
  </style>
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Register</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white flex flex-col items-center justify-center min-h-screen">
  <div class="bg-zinc-800 p-10 rounded-lg shadow-md w-full max-w-md">
    <h2 class="text-3xl font-bold mb-5">Create Account</h2>

    <% if (typeof error !== 'undefined') { %>
      <p class="mb-4 text-red-400 font-semibold"><%= error %></p>
    <% } %>

    <form action="/register" method="post" class="space-y-4">
      <input class="input" type="text" name="username" placeholder="Username" required 
        value="<%= typeof oldData !== 'undefined' ? oldData.username : '' %>" />

      <input class="input" type="text" name="name" placeholder="Name" required 
        value="<%= typeof oldData !== 'undefined' ? oldData.name : '' %>" />

      <input class="input" type="number" name="age" min="1" max="120" placeholder="Age" required 
        value="<%= typeof oldData !== 'undefined' ? oldData.age : '' %>" />

      <input class="input" type="email" name="email" placeholder="Email" required 
        value="<%= typeof oldData !== 'undefined' ? oldData.email : '' %>" />

      <input class="input" type="password" name="password" placeholder="Password" required />

      <button class="btn-primary w-full" type="submit">Register</button>
    </form>

    <p class="text-sm mt-4 text-zinc-400">Already have an account? 
      <a href="/login" class="text-blue-400 underline">Login</a>
    </p>
  </div>

  <style>
    .input {
      width: 100%;
      padding: 0.5rem;
      background: transparent;
      border: 1px solid #444;
      border-radius: 0.375rem;
      outline: none;
    }
    .btn-primary {
      background-color: #3b82f6;
      padding: 0.5rem;
      border-radius: 0.375rem;
      font-weight: bold;
    }
    .btn-primary:hover {
      background-color: #2563eb;
    }
  </style>
</body>
</html>
